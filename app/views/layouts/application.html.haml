!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : 'rubydoo'
    %meta{:name => "description", :content => "#{content_for?(:description) ? yield(:description) : 'rubydoo'}"}
    = stylesheet_link_tag "application", media: "all"
    = javascript_include_tag "vendor/custom.modernizr"
    = csrf_meta_tags
  %body
    = flash.notice
    = flash.alert
    = yield
    = javascript_include_tag "application"
    = yield :scripts